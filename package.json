{
  "name": "tailwind-profile",
  "version": "1.0.0",
  "description": "demo page built using tailwind",
  "main": "index.js",
  "scripts": {
    "watch": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.0.7"
  }
}
